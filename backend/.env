ENABLE_TEST_FAILURE_HOOKS=true
# MySQL (Payroll) configuration
MYSQL_HOST=127.0.0.1
MYSQL_PORT=3306
MYSQL_DATABASE=PAYROLL
MYSQL_USER=root
MYSQL_PASSWORD=Hieuthi22032005@

# Optional: FRONTEND origin for CORS
FRONTEND_ORIGIN=http://localhost:5174

# SQL Server database (HUMAN)
SQLSERVER_DATABASE=HUMAN
# SQL Server instance (use instance name if not default)
SQLSERVER_SERVER=.\SQLEXPRESS
USE_TRUSTED_CONNECTION=true
SQLSERVER_ENCRYPT=no
# When true, allow falling back to a local SQLite file if SQL Server cannot be reached (dev only).
# Set to 'true' on developer machines when you want the app to create a local fallback DB.
FALLBACK_TO_SQLITE=true
